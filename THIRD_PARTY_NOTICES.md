# Third-party notices

## ankerl::unordered_dense 4.8.1

The production key-value store uses `ankerl::unordered_dense`, obtained through
the vcpkg `unordered-dense` port. It is licensed under the MIT License.

Upstream: https://github.com/martinus/unordered_dense

Install the pinned project dependency before building:

```powershell
vcpkg install --triplet x64-mingw-dynamic
```
